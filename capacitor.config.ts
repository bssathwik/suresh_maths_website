import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sureshmathsmaterial.app',
  appName: 'Suresh Maths Material',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
